<h1>Wine</h1>
<p>Available as <%= formatted_time(@available_at) %></p>
<%= page_entries_info @wine %>
<ol>
<% @wine.each do |wine| %>
  <li><%= link_to(wine.name, wine) %> by <%= wine.country %></li>
<% end %>
</ol>
<%= paginate @wine %>
